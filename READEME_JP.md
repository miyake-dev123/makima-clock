Markdown
# 高機能TFTキャラクタークロック (RP2040搭載版)

本製品は、Raspberry Pi Picoの互換ミニボード (RP2040) をベースに、高解像度TFT液晶、高精度リアルタイムクロック (RTC)、および環境センサーを組み合わせた、据え置き型の高機能デジタル時計です。
指定した時間での背景画像の自動切り替えに加え、タクトスイッチによる手動モード切り替え、時刻合わせ、および祝祭日・記念日のカラー表示に対応しています。

---

## 1. 主な機能

1. **マルチモード背景表示**
   タクトスイッチ（アジャストボタン）の操作により、以下の4つのモードを循環切り替えできます。
   * モード0：マキマ背景 ＋ 文字表示（あり）
   * モード1：チェンソーマン背景 ＋ 文字表示（あり）
   * モード2：チェンソーマン背景 ＋ 文字表示（なし・画像鑑賞モード）
   * モード3：マキマ背景 ＋ 文字表示（なし・画像鑑賞モード）
2. **オンボード時刻合わせ（3ボタン制御）**
   外部機器を接続することなく、本体のボタン操作のみでカレンダーおよび時刻を修正可能です。
3. **環境モニター**
   温度および気圧をリアルタイムで測定し、0.1単位の変化を検知して滑らかに画面を更新します。
4. **祝祭日・記念日の色識別表示**
   LittleFS内のバイナリデータに基づき、特定の祝日や記念日に文字色を自動で変更します。
5. **ハングアップ防止（WDT監視）**
   ウォッチドッグタイマにより、万が一システムがフリーズした際も数秒以内に自動再起動して復帰します。

---

## 2. ビルド環境・使用ライブラリ

本プロジェクトは **Arduino IDE 2.x** または **PlatformIO** 環境にてビルド可能です。

### ■ 使用ライブラリ一覧
| ライブラリ名 | バージョン | 用途 |
| :--- | :--- | :--- |
| `Adafruit_GFX_Library` | 1.11.9 以降 | グラフィック基本描画 |
| `Adafruit_ST7789_Library` | 1.10.3 以降 | TFT液晶ディスプレイ制御 |
| `U8g2_for_Adafruit_GFX` | 1.8.0 以降 | カスタムフォント・日本語描画 |
| `RTClib` (Adafruit版) | 2.1.4 以降 | 高精度RTC (DS3231) 制御 |
| `Adafruit_BMP280_Library` | 2.6.8 以降 | 温度・気圧センサー制御 |

---

## 3. パーツリスト

| 部品名 | 数量 | 備考 |
| :--- | :--- | :--- |
| RP2040 ミニボード | 1 | マイコンボード（実物ピン配置準拠） |
| 2.0インチ TFT液晶モジュール (ST7789搭載) | 1 | 320x240ピクセル、SPI接続 |
| DS3231 搭載 高精度RTCモジュール | 1 | I2C接続 (Address: 0x68) |
| BMP280 搭載 温度・気圧センサーモジュール | 1 | I2C接続 (Address: 0x76 または 0x77) |
| タクトスイッチ | 3 | モード切り替え・時刻合わせ用 |
| ブレッドボード / ジャンパ線 | 一式 | 配線用 |

---

## 4. 回路図（接続仕様）

本システムを構築する際の、RP2040ミニボードと各周辺デバイスの接続関係です。
※各タクトスイッチの反対側の脚は、すべてボード左下の **GND** ピンに接続してください。

### ■ 信号線 接続一覧

#### ① TFT液晶ディスプレイ (ST7789 SPI0)
* GPIO 15 ── TFT_RST (Reset) 
* GPIO 16 ── TFT_DC (Data/Command)
* GPIO 17 ── TFT_CS (Chip Select)
* GPIO 18 ── TFT_SCLK (SPI0_SCK)
* GPIO 19 ── TFT_MOSI (SPI0_TX)

#### ② リアルタイムクロック (DS3231 [I2C0])
* GPIO 4  ── I2C0_SCL (Serial Clock) ※要外部プルアップ抵抗
* GPIO 5  ── I2C0_SDA (Serial Data)  ※要外部プルアップ抵抗

#### ③ 環境センサー (BMP280 [I2C1])
* GPIO 6  ── I2C1_SDA (Serial Data)
* GPIO 7  ── I2C1_SCL (Serial Clock)

#### ④ 操作用タクトスイッチ (内部プルアップ使用)
* GPIO 22 ── アジャストボタン (3秒長押しで時刻合わせモード移行/通常モード復帰)
* GPIO 20 ── セレクトボタン (時刻合わせ時の項目選択：年月日時分秒)
* GPIO 21 ── カウントアップボタン (選択された項目の数値を+1)

---

### ■ 実物基板レイアウト準拠 配線イメージ図

```text
                        RP2040 ミニボード（最終配線確定版）
                                    回路図
                    
    【左側接続デバイス】          +---------------+          【右側接続デバイス】
                 (不明)  [--| (不明)    (15) |--]  15  ---> [TFT_RST] 液晶
         [TFT_DC] <--- 16  [--| 16         14 |--]  14
         [TFT_CS] <--- 17  [--| 17         13 |--]  13
       [TFT_SCLK] <--- 18  [--| 18         12 |--]  12
       [TFT_MOSI] <--- 19  [--| 19         11 |--]  11
   [セレクトボタン] <--- 20  [--| 20         10 |--]  10
 [カウントアップ] <--- 21  [--| 21          9 |--]  9
 [アジャストボタン] <--- 22  [--| 22          8 |--]  8
                         23  [--| 23          7 |--]  7  ---> [I2C1_SCL] BMP280
                         24  [--| 24          6 |--]  6  ---> [I2C1_SDA] BMP280
                         25  [--| 25          5 |--]  5  ---> [I2C0_SDA] RTC (DS3231)
                          0  [--|  0          4 |--]  4  ---> [I2C0_SCL] RTC (DS3231)
                          1  [--|  1          3 |--]  3
                          2  [--|  2          2 |--]  2
                          3  [--|  3          1 |--]  1
                             [--|             0 |--]  0
          [共通GND] <--- GND [--| GND        5V |--]  5V ──> 各デバイスの VCC (5V)
                 3V3 <--- 3V3 [--| 3V3       GND |--]  GND ──> 各デバイスの GND
                             +-------+-------+
                                     |
                                   [USB-C]
5. 壁紙バイナリの作成とアップロード方法
液晶への高速描画（遅延排除）のため、画像は解凍済みのRGB565（Big-Endian）形式の生のバイナリデータ（.bin）としてLittleFSに格納します。これによりマイコン側での展開処理が不要となり、瞬時の切り替えが可能です。

■ ffmpegによる画像変換コマンド
解像度 320x240 の元画像（input.jpg や input.png）を変換する場合、以下のコマンドを実行します。

Bash
ffmpeg -i input.jpg -f rawvideo -pix_fmt rgb565be makima.bin
-pix_fmt rgb565be：ST7789液晶の仕様（上位バイト先出し）に合わせ、RGB565のBig-Endian形式を指定しています。

■ 壁紙ファイルのアップロード
生成された makima.bin および chanesouman.bin を、スケッチフォルダ内の data フォルダに配置します。

開発環境のアップロードツールを実行し、Picoのフラッシュメモリ（LittleFS）へ書き込みます。

6. 祝祭日・記念日データの格納形式
祝祭日や記念日のデータは、1日あたり6バイトの固定長構造体バイナリとしてLittleFS内に保存されます。

■ データ構造 (C言語表記)
C++
struct DateTime {
  uint16_t year;        // 2バイト：対象年 (0指定で毎年固定の記念日扱い)
  uint8_t month;        // 1バイト：対象月 (1〜12)
  uint8_t day;          // 1バイト：対象日 (1〜31)
  uint16_t disp_color;  // 2バイト：その日の表示色定義 (RGB565形式)
};
7. 使い方と操作方法
■ 通常モード
アジャストボタンを短押し： 画面モード（モード0〜3）が順番に切り替わります。

■ 時刻合わせモード
モード移行： 通常モード中にアジャストボタンを3秒以上長押しすると、時刻合わせモードに移行します。

項目の選択： セレクトボタンを押すたびに、変更対象のユニットが「年 ──> 月 ──> 日 ──> 時 ──> 分 ──> 秒」の順に切り替わります。

値の変更： カウントアップボタンを押すと、選択されているユニットの数値が1ずつ増加します。

設定の確定： 再びアジャストボタンを3秒以上長押しすると、設定値が外付けRTCに書き込まれ、通常モードに戻ります。
# makima_clock



### 配線一覧 (Pico GPIO ⇔ 周辺機器)

再現性のため、配線に迷ったら必ずここを見る。

### I2C0 — RTC (DS3231)

| Pico GPIO | 信号 | 接続先          |
|-----------|------|-----------------|
| GP4       | SDA  | DS3231 SDA      |
| GP5       | SCL  | DS3231 SCL      |

> 2026/07/26 確定。配線実測とコード側の`Wire.setSDA/setSCL`の対応が取れていることを確認済み。

> **参考**: DS3231モジュール上のAT24C32 EEPROM(本プロジェクトでは未使用)は実測で `0x50` に応答した。
> データシート上のデフォルト`0x57`はA0/A1/A2ピンがすべてVCCプルアップされている場合の値であり、
> 基板によってはA0/A1/A2がGND側に配線されているため実際のアドレスは基板依存。使う場合は必ずI2Cスキャナーで確認すること。

### I2C1 — 環境センサー (BMP280)

| Pico GPIO | 信号 | 接続先          |
|-----------|------|-----------------|
| GP6       | SDA  | BMP280 SDA      |
| GP7       | SCL  | BMP280 SCL      |

BMP280はアドレス0x76または0x77のどちらでも自動判定 (`bmp.begin(0x76) / bmp.begin(0x77)`)。

### SPI0 — TFT液晶 (ST7789, 320x240)

| Pico GPIO | 信号        | 接続先        |
|-----------|-------------|---------------|
| GP15      | RST         | TFT RESET     |
| GP14      | DC (data/command) | TFT DC  |
| GP17      | CS          | TFT CS        |
| GP18      | SCLK (SCK)  | TFT SCK       |
| GP19      | MOSI (TX)   | TFT MOSI      |

> **注意 (解決済み)**: RP2040のSPI0はデフォルトでMISO=GP16を自動的に占有する。
> 当初DCピンをGP16に割り当てていたため、SPI0の初期化時にMISO機能へ横取りされ、
> TFTに何も表示されない不具合が発生した(バックライトは点灯するがコマンドが通らない状態)。
> ソフト側でMISOを逃がす方法も検討したが、最終的に**DCピンをGP14へ変更する**方式で解決。
> GP14はI2C/SPI/ボタンのいずれとも競合しないため、配線をGP14に繋ぎ直すだけで良い。

### タクトスイッチ

| Pico GPIO | 役割          |
|-----------|---------------|
| GP20      | SELECT (編集項目切替) |
| GP21      | UP (数値インクリメント) |
| GP22      | ADJUST (長押しで時刻編集モード、短押しで表示モード切替) |

## 電源・その他

- LittleFSで背景画像(`/makima.bin`, `/chanesouman.bin`)と祝日データ(`/holidays.bin`)を管理。
- Watchdogタイマーは5000ms、`setup()`の全初期化完了後に有効化(初期化中の誤リセット防止のため)。
- BMP280が検出できない場合も`bmpOk`フラグにより非致命的に継続動作する。

## 動作確認ステータス

- 2026/07/26時点、上記の配線・コードで **RTC(DS3231)・BMP280・TFT表示すべて正常動作を確認済み**。
- I2Cスキャナー(別ファイル)でRTC/BMP280両方のアドレスACKを確認、タクトスイッチでの再スキャンも動作。

## 既知のTODO / 検討中の項目

- `handleButtons()`内の自動背景切り替え(`displayMode == 0 || displayMode == 1`のタイマー処理)が、
  ボタンで選択した表示モードを上書きしてしまう場合がある。要修正。
- 予期しない再起動が発生する場合があり、Watchdogタイムアウトか電源(ブラウンアウト)が疑われる。要継続調査。

## 変更履歴 (抜粋)

- I2C0/I2C1のピン設定・タイムアウト・RTCリトライ・BMP280非致命化・watchdog有効化タイミング修正
- TFT_DCピンをGP16→GP14へ変更し、SPI0のMISOピンとの競合を解消(TFT無表示問題を解決)
- 環境センサーのステータスレポート出力を`firstRun`依存から分単位の独立判定(`lastReportedMinute`)に変更し、背景切替のタイミングに引きずられないよう修正
■ デバッグ機能（開発者向け）
PCとUSB接続し、シリアルモニタ（115200bps）から YYYYMMDD（例: 20260607）と入力して送信することで、強制的にRTCのシステム日付を変更し、祝祭日表示などのテストを行うことができます。
